# Optional. The project uses FRED's public CSV endpoint by default and does not require a key.
FRED_API_KEY=
# Set to 1 to refresh downloads even when a cached file exists.
COORDWATCH_FORCE_DOWNLOAD=0
